NVCC = nvcc
OUTPUT_DIR = bin

# EX_FILES = $(wildcard ex*.cu)
EX_FILES = ex_2_matrix_multiplication_uint.cu
EXECUTABLES = $(patsubst %.cu, $(OUTPUT_DIR)/%, $(EX_FILES))

all: $(EXECUTABLES)

# $(OUTPUT_DIR)/%: %.cu
# 	$(NVCC) -o $@ $<


$(OUTPUT_DIR)/ex_2_matrix_multiplication_uint: ex_2_matrix_multiplication_uint.cu lite_*.cu
	$(NVCC) -o $< $@ 

clean:
	rm -f $(EXECUTABLES)